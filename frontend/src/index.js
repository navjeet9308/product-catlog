import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css'; // Optional: if you have styles in an index.css file
import App from './App';
// import reportWebVitals from './reportWebVitals'; // Optional: for performance measurement

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// Optional: for performance reporting
// reportWebVitals();
